runtime: go
api_version: go1
default_expiration: 300s

handlers:
  posts:
    http_headers:
      Link: "</static/css/style.css>; rel=preload; as=style"
  post-static:
    url: /images/
    static_dir: spec/dest/images
